import { theme } from '../constants';

export const _Logo = {
  padding: '10px'
};

export const _Link = {
  margin: '0 20px',
  color: theme.textColor,
  fontWeight: 'bold',
  transition: '1s'
};

export const _Header = {
  borderBottom: `3px solid ${theme.color}`
};

export const _Active_Link = {
  color: theme.color
};
